{
  "name": "sm-investments-portal",
  "version": "1.0.0",
  "description": "S&M Investments Member Research Portal",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required'"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
